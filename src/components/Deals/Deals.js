import React from 'react';

const Deals = () => {
    return (
        <div className="container font-link">
            <h3 className="text-center">There is no deal for Today</h3>
        </div>
    );
};

export default Deals;